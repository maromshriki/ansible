---
plugin: amazon.aws.aws_ec2
regions:
 - us-east-1 # Replace with your region
keyed_groups:
 - prefix: tag
   key: tags.Name
filters:
  tag:Name: "mysql-test-aws-marom-01" # Filter instances with the tag
  tag:Service: "mysql" # Filter instances with the tag
  tag:Version: "5.7" # Filter instances with the tag
  tag:Restart: "Sunday at 6:00 am" # Filter instances with the tag

